import React from 'react';
import Container from '../components/Bits/Container/Container';

const Home = () => {
  return (
    <Container>
      <p>
        This is the home page. We can put basic instructions/info for this
        application here. Click the hamburger icon to view the navigation
        drawer; select a topic to learn more.
      </p>
    </Container>
  );
};

export default Home;
